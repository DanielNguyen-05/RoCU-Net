# A separate protocol, for a new controlled comparison with all baselines.
# Never initialize this from the old ColonDB run: it has seen new holdout images.
base_config: cvc_colondb_v2.yaml
experiment:
  name: rocu_cvc_colondb_grouped_seed42
data:
  split_source: null
  group_identical_images: true
training:
  init_checkpoint: null
